import React from 'react';
import { shallow } from 'enzyme';

import RQM from './RQM';

describe('<RQM>', () => {
  
  let wrapper;
  beforeEach(function() {
    wrapper = shallow(<RQM />);
  });

});
